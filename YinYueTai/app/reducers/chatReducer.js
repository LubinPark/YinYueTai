import * as types from '../actions/actionTypes'

const initialState = {
  data: []
}

let Chat = (state = initialState, action={}) => {
  switch (action.type) {
    default:
      return state
  }
}

export default Chat
